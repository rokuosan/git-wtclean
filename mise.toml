[tools]
go = "1.26.5"
golangci-lint = "2.13.2"
goreleaser = "latest"
shellcheck = "latest"

[tasks.format]
description = "Format Go source files"
run = [
  "gofmt -w ./cmd ./internal",
  "golangci-lint fmt",
]

[tasks.lint]
description = "Run Go tests and linters"
run = [
  "go test ./...",
  "go vet ./...",
  "golangci-lint config verify",
  "golangci-lint run",
]
